/* function sumNoDuplicates(numList) {
  if (numList.length === 0) return 0;
  let setList = new Set(numList);
  return setList;
}

console.log(sumNoDuplicates([3, 4, 3, 6]));
 */
/* function topRungetter(playerInfo) {
  let topRuner = playerInfo[0][0];
  let topRun = playerInfo[0][1];

  for (let x = 1; x < playerInfo.length; x++) {
    if (topRun < playerInfo[x][1]) {
      topRun = playerInfo[x][1];
      topRuner = playerInfo[x][0];
    }
  }
  return topRuner;
}

const playerInfo = [
  ["atik", 77], //0
  ["jony", 80], //1
  ["usuf", 87], //2
  ["arif", 82], //3
];
console.log(topRungetter(playerInfo)); */
